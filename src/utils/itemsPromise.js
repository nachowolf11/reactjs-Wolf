const itemsPromise = (listado) => {
    return new Promise ((resolve,reject) => {
        if (true) {
                resolve(listado)
        } else {
            reject();
        }
    })
}

export default itemsPromise;